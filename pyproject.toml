[project]
name = "astro-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Kehinde Olalusi", email = "101356362+tiloye@users.noreply.github.com" }
]
requires-python = ">=3.12"
dependencies = [
    "duckdb==0.10.3",
    "pandas>=3.0.3",
    "pyarrow>=24.0.0",
    "tabulate==0.9.0",
]

[project.scripts]
astro-demo = "astro_demo:main"

[build-system]
requires = ["uv_build>=0.11.14,<0.12.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
